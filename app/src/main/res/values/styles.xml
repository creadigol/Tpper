<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCustomToolbarTheme" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColorPrimary">@color/colorWhite</item>
        <item name="android:textColorSecondary">@color/colorWhite</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FacebookLoginButton">
        <item name="android:background">@drawable/facebook</item>
    </style>
    <style name="dialog_animation">
        <item name="android:windowEnterAnimation">@anim/left</item>
        <item name="android:windowExitAnimation">@anim/right</item>
    </style>
    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/food_rating_bar_fullsmall</item>
        <item name="android:minHeight">20dp</item>
        <item name="android:layout_width">84dp</item>
        <item name="android:maxHeight">20dip</item>
    </style>

    <style name="dollarRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/smalldollar_ratingbar_main</item>
        <item name="android:minHeight">20dp</item>
        <item name="android:layout_width">80dp</item>
        <item name="android:maxHeight">20dp</item>
    </style>
    <style name="dollarSmallRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/dollarsmall_ratingbar_main</item>
        <item name="android:minHeight">18dp</item>
        <item name="android:layout_width">64dp</item>
        <item name="android:maxHeight">18dp</item>
    </style>
    <style name="foodRatingBarBig" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/food_rating_bar_full</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:layout_width">126dp</item>
        <item name="android:maxHeight">30dip</item>
    </style>
    <style name="ratingBardetails" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbardetailsmain</item>
        <item name="android:minHeight">25dp</item>
        <item name="android:layout_width">102dp</item>
        <item name="android:maxHeight">25dip</item>
    </style>
</resources>
